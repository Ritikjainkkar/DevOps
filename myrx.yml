apiVersion: v1
kind: ReplicationController
metadata:
  name: myrc
spec:
  replicas: 3
  selector:
    app: myrc
  template:
    metadata:
      name: myrc
      labels:
        app: myrc
    spec:
      containers:
        - name: myrc
          image: nginx
          ports:
            - containerPort: 8080
